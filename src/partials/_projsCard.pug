.projs-card
    .projs-overline-all 
        span.overline.projs-dscplne #{projsDscplne}
        span.overline.projs-order 01 / 08
    a.projs-titlelink(href='/the-valivaryus-project')
        h2.projs-name #{projsName} 
        //- proj-arrow will be taken care of with pseudo after element
    img.projs-img(src=`${projsFtImg}` alt="A white background with an all-in-one desktop computer.")